-- Query 1
Retrieve employee name in proper case, calculate total income including bonus, extract joining year, and classify employees as Junior, Mid, or Senior based on experience.

-- Query 2
Display customer delivery details including delivery days, truncated order amount, and determine delivery status (PENDING, SAME-DAY, DELAYED, NORMAL).

-- Query 3
Fetch customer spending details including formatted name, purchase month, rounded amount, absolute value, and categorize spending type (High, Medium, Low Spender).

-- Query 4
Extract email domain from user email, calculate subscription duration, format fee, remaining days, and determine subscription status (ACTIVE, EXPIRING SOON, EXPIRED).

-- Query 5
Compute EMI for loans, calculate years since loan started, and classify risk category based on interest rate (High, Medium, Low Risk).

-- Query 6
Calculate attendance percentage, absent days, month of record, and classify attendance status (Excellent, Average, Poor).

-- Query 7
Analyze product sales to calculate discount amount, discount percentage, sale day, and classify discount status (Valid Discount, Overpriced, No Discount).

-- Query 8
Fetch insurance policy details including policy duration, remaining days, rounded premium, and classify policy status (Expired, Long Term, Mid Term).

-- Query 9
Calculate employee salary increment based on rating, compute new salary, years since last hike, and classify increment status (High, Moderate, No Increment).

-- Query 10
Analyze bank accounts to extract branch name, days since last transaction, absolute balance, and classify account status (Active, Dormant, Overdrawn).
  
